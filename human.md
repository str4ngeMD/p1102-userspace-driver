in a macos arm64 VM in UTM,


install homebrew

https://brew.sh


close the terminal, open a new one, (to load `brew` to the $PATH)

then run 
`brew install ghostscript libusb`


`git clone https://github.com/str4ngeMD/p1102-userspace-driver.git`

cd into it

```
% python3 -m venv venv
% ./venv/bin/pip install pyusb
```

